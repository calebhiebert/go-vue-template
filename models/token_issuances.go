// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TokenIssuance is an object representing the database table.
type TokenIssuance struct {
	ID        string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID    string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	IPAddress string    `boil:"ip_address" json:"ip_address" toml:"ip_address" yaml:"ip_address"`
	CreatedAt null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`

	R *tokenIssuanceR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tokenIssuanceL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TokenIssuanceColumns = struct {
	ID        string
	UserID    string
	IPAddress string
	CreatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	IPAddress: "ip_address",
	CreatedAt: "created_at",
}

var TokenIssuanceTableColumns = struct {
	ID        string
	UserID    string
	IPAddress string
	CreatedAt string
}{
	ID:        "token_issuances.id",
	UserID:    "token_issuances.user_id",
	IPAddress: "token_issuances.ip_address",
	CreatedAt: "token_issuances.created_at",
}

// Generated where

var TokenIssuanceWhere = struct {
	ID        whereHelperstring
	UserID    whereHelperstring
	IPAddress whereHelperstring
	CreatedAt whereHelpernull_Time
}{
	ID:        whereHelperstring{field: "\"token_issuances\".\"id\""},
	UserID:    whereHelperstring{field: "\"token_issuances\".\"user_id\""},
	IPAddress: whereHelperstring{field: "\"token_issuances\".\"ip_address\""},
	CreatedAt: whereHelpernull_Time{field: "\"token_issuances\".\"created_at\""},
}

// TokenIssuanceRels is where relationship names are stored.
var TokenIssuanceRels = struct {
	User string
}{
	User: "User",
}

// tokenIssuanceR is where relationships are stored.
type tokenIssuanceR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*tokenIssuanceR) NewStruct() *tokenIssuanceR {
	return &tokenIssuanceR{}
}

// tokenIssuanceL is where Load methods for each relationship are stored.
type tokenIssuanceL struct{}

var (
	tokenIssuanceAllColumns            = []string{"id", "user_id", "ip_address", "created_at"}
	tokenIssuanceColumnsWithoutDefault = []string{"id", "user_id", "ip_address"}
	tokenIssuanceColumnsWithDefault    = []string{"created_at"}
	tokenIssuancePrimaryKeyColumns     = []string{"id"}
)

type (
	// TokenIssuanceSlice is an alias for a slice of pointers to TokenIssuance.
	// This should almost always be used instead of []TokenIssuance.
	TokenIssuanceSlice []*TokenIssuance
	// TokenIssuanceHook is the signature for custom TokenIssuance hook methods
	TokenIssuanceHook func(context.Context, boil.ContextExecutor, *TokenIssuance) error

	tokenIssuanceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tokenIssuanceType                 = reflect.TypeOf(&TokenIssuance{})
	tokenIssuanceMapping              = queries.MakeStructMapping(tokenIssuanceType)
	tokenIssuancePrimaryKeyMapping, _ = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, tokenIssuancePrimaryKeyColumns)
	tokenIssuanceInsertCacheMut       sync.RWMutex
	tokenIssuanceInsertCache          = make(map[string]insertCache)
	tokenIssuanceUpdateCacheMut       sync.RWMutex
	tokenIssuanceUpdateCache          = make(map[string]updateCache)
	tokenIssuanceUpsertCacheMut       sync.RWMutex
	tokenIssuanceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tokenIssuanceBeforeInsertHooks []TokenIssuanceHook
var tokenIssuanceBeforeUpdateHooks []TokenIssuanceHook
var tokenIssuanceBeforeDeleteHooks []TokenIssuanceHook
var tokenIssuanceBeforeUpsertHooks []TokenIssuanceHook

var tokenIssuanceAfterInsertHooks []TokenIssuanceHook
var tokenIssuanceAfterSelectHooks []TokenIssuanceHook
var tokenIssuanceAfterUpdateHooks []TokenIssuanceHook
var tokenIssuanceAfterDeleteHooks []TokenIssuanceHook
var tokenIssuanceAfterUpsertHooks []TokenIssuanceHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TokenIssuance) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TokenIssuance) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TokenIssuance) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TokenIssuance) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TokenIssuance) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TokenIssuance) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TokenIssuance) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TokenIssuance) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TokenIssuance) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tokenIssuanceAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTokenIssuanceHook registers your hook function for all future operations.
func AddTokenIssuanceHook(hookPoint boil.HookPoint, tokenIssuanceHook TokenIssuanceHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		tokenIssuanceBeforeInsertHooks = append(tokenIssuanceBeforeInsertHooks, tokenIssuanceHook)
	case boil.BeforeUpdateHook:
		tokenIssuanceBeforeUpdateHooks = append(tokenIssuanceBeforeUpdateHooks, tokenIssuanceHook)
	case boil.BeforeDeleteHook:
		tokenIssuanceBeforeDeleteHooks = append(tokenIssuanceBeforeDeleteHooks, tokenIssuanceHook)
	case boil.BeforeUpsertHook:
		tokenIssuanceBeforeUpsertHooks = append(tokenIssuanceBeforeUpsertHooks, tokenIssuanceHook)
	case boil.AfterInsertHook:
		tokenIssuanceAfterInsertHooks = append(tokenIssuanceAfterInsertHooks, tokenIssuanceHook)
	case boil.AfterSelectHook:
		tokenIssuanceAfterSelectHooks = append(tokenIssuanceAfterSelectHooks, tokenIssuanceHook)
	case boil.AfterUpdateHook:
		tokenIssuanceAfterUpdateHooks = append(tokenIssuanceAfterUpdateHooks, tokenIssuanceHook)
	case boil.AfterDeleteHook:
		tokenIssuanceAfterDeleteHooks = append(tokenIssuanceAfterDeleteHooks, tokenIssuanceHook)
	case boil.AfterUpsertHook:
		tokenIssuanceAfterUpsertHooks = append(tokenIssuanceAfterUpsertHooks, tokenIssuanceHook)
	}
}

// OneG returns a single tokenIssuance record from the query using the global executor.
func (q tokenIssuanceQuery) OneG(ctx context.Context) (*TokenIssuance, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single tokenIssuance record from the query.
func (q tokenIssuanceQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TokenIssuance, error) {
	o := &TokenIssuance{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for token_issuances")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TokenIssuance records from the query using the global executor.
func (q tokenIssuanceQuery) AllG(ctx context.Context) (TokenIssuanceSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all TokenIssuance records from the query.
func (q tokenIssuanceQuery) All(ctx context.Context, exec boil.ContextExecutor) (TokenIssuanceSlice, error) {
	var o []*TokenIssuance

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TokenIssuance slice")
	}

	if len(tokenIssuanceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TokenIssuance records in the query, and panics on error.
func (q tokenIssuanceQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all TokenIssuance records in the query.
func (q tokenIssuanceQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count token_issuances rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table, and panics on error.
func (q tokenIssuanceQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q tokenIssuanceQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if token_issuances exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *TokenIssuance) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "\"users\"")

	return query
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tokenIssuanceL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTokenIssuance interface{}, mods queries.Applicator) error {
	var slice []*TokenIssuance
	var object *TokenIssuance

	if singular {
		object = maybeTokenIssuance.(*TokenIssuance)
	} else {
		slice = *maybeTokenIssuance.(*[]*TokenIssuance)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &tokenIssuanceR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tokenIssuanceR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, args...),
		qmhelper.WhereIsNull(`users.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(tokenIssuanceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.TokenIssuances = append(foreign.R.TokenIssuances, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.TokenIssuances = append(foreign.R.TokenIssuances, local)
				break
			}
		}
	}

	return nil
}

// SetUserG of the tokenIssuance to the related item.
// Sets o.R.User to related.
// Adds o to related.R.TokenIssuances.
// Uses the global database handle.
func (o *TokenIssuance) SetUserG(ctx context.Context, insert bool, related *User) error {
	return o.SetUser(ctx, boil.GetContextDB(), insert, related)
}

// SetUser of the tokenIssuance to the related item.
// Sets o.R.User to related.
// Adds o to related.R.TokenIssuances.
func (o *TokenIssuance) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"token_issuances\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, tokenIssuancePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &tokenIssuanceR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			TokenIssuances: TokenIssuanceSlice{o},
		}
	} else {
		related.R.TokenIssuances = append(related.R.TokenIssuances, o)
	}

	return nil
}

// TokenIssuances retrieves all the records using an executor.
func TokenIssuances(mods ...qm.QueryMod) tokenIssuanceQuery {
	mods = append(mods, qm.From("\"token_issuances\""))
	return tokenIssuanceQuery{NewQuery(mods...)}
}

// FindTokenIssuanceG retrieves a single record by ID.
func FindTokenIssuanceG(ctx context.Context, iD string, selectCols ...string) (*TokenIssuance, error) {
	return FindTokenIssuance(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTokenIssuance retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTokenIssuance(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TokenIssuance, error) {
	tokenIssuanceObj := &TokenIssuance{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"token_issuances\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tokenIssuanceObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from token_issuances")
	}

	if err = tokenIssuanceObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tokenIssuanceObj, err
	}

	return tokenIssuanceObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TokenIssuance) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TokenIssuance) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no token_issuances provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tokenIssuanceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tokenIssuanceInsertCacheMut.RLock()
	cache, cached := tokenIssuanceInsertCache[key]
	tokenIssuanceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tokenIssuanceAllColumns,
			tokenIssuanceColumnsWithDefault,
			tokenIssuanceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"token_issuances\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"token_issuances\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into token_issuances")
	}

	if !cached {
		tokenIssuanceInsertCacheMut.Lock()
		tokenIssuanceInsertCache[key] = cache
		tokenIssuanceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TokenIssuance record using the global executor.
// See Update for more documentation.
func (o *TokenIssuance) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the TokenIssuance.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TokenIssuance) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tokenIssuanceUpdateCacheMut.RLock()
	cache, cached := tokenIssuanceUpdateCache[key]
	tokenIssuanceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tokenIssuanceAllColumns,
			tokenIssuancePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update token_issuances, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"token_issuances\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, tokenIssuancePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, append(wl, tokenIssuancePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update token_issuances row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for token_issuances")
	}

	if !cached {
		tokenIssuanceUpdateCacheMut.Lock()
		tokenIssuanceUpdateCache[key] = cache
		tokenIssuanceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q tokenIssuanceQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q tokenIssuanceQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for token_issuances")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for token_issuances")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TokenIssuanceSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TokenIssuanceSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenIssuancePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"token_issuances\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, tokenIssuancePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in tokenIssuance slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all tokenIssuance")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TokenIssuance) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TokenIssuance) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no token_issuances provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tokenIssuanceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tokenIssuanceUpsertCacheMut.RLock()
	cache, cached := tokenIssuanceUpsertCache[key]
	tokenIssuanceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			tokenIssuanceAllColumns,
			tokenIssuanceColumnsWithDefault,
			tokenIssuanceColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			tokenIssuanceAllColumns,
			tokenIssuancePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert token_issuances, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(tokenIssuancePrimaryKeyColumns))
			copy(conflict, tokenIssuancePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"token_issuances\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tokenIssuanceType, tokenIssuanceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert token_issuances")
	}

	if !cached {
		tokenIssuanceUpsertCacheMut.Lock()
		tokenIssuanceUpsertCache[key] = cache
		tokenIssuanceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TokenIssuance record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TokenIssuance) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single TokenIssuance record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TokenIssuance) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TokenIssuance provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tokenIssuancePrimaryKeyMapping)
	sql := "DELETE FROM \"token_issuances\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from token_issuances")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for token_issuances")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q tokenIssuanceQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q tokenIssuanceQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no tokenIssuanceQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from token_issuances")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for token_issuances")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TokenIssuanceSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TokenIssuanceSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tokenIssuanceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenIssuancePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"token_issuances\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tokenIssuancePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from tokenIssuance slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for token_issuances")
	}

	if len(tokenIssuanceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TokenIssuance) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no TokenIssuance provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TokenIssuance) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTokenIssuance(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TokenIssuanceSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty TokenIssuanceSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TokenIssuanceSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TokenIssuanceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tokenIssuancePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"token_issuances\".* FROM \"token_issuances\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tokenIssuancePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TokenIssuanceSlice")
	}

	*o = slice

	return nil
}

// TokenIssuanceExistsG checks if the TokenIssuance row exists.
func TokenIssuanceExistsG(ctx context.Context, iD string) (bool, error) {
	return TokenIssuanceExists(ctx, boil.GetContextDB(), iD)
}

// TokenIssuanceExists checks if the TokenIssuance row exists.
func TokenIssuanceExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"token_issuances\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if token_issuances exists")
	}

	return exists, nil
}

type TokenIssuanceColumnData struct {
	ID        TableField `json:"id"`
	UserID    TableField `json:"user_id"`
	IPAddress TableField `json:"ip_address"`
	CreatedAt TableField `json:"created_at"`
}

var TokenIssuanceColumnsData TokenIssuanceColumnData = TokenIssuanceColumnData{
	ID: TableField{
		Name:       "id",
		Type:       "string",
		HasDefault: false,
		Nullable:   false,
	},
	UserID: TableField{
		Name:       "user_id",
		Type:       "string",
		HasDefault: false,
		Nullable:   false,
	},
	IPAddress: TableField{
		Name:       "ip_address",
		Type:       "string",
		HasDefault: false,
		Nullable:   false,
	},
	CreatedAt: TableField{
		Name:       "created_at",
		Type:       "null.Time",
		HasDefault: true,
		Nullable:   true,
	},
}
